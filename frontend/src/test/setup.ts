// Vitest global setup — runs once before the test suites.
// Registers the @testing-library/jest-dom matchers on Vitest's `expect`
// (e.g. toBeInTheDocument, toHaveClass) for future component tests.
import '@testing-library/jest-dom/vitest'
